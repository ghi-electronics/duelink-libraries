# Build
npm install parcel-bundler --save-dev
npm run build

# Test local
npm link

# Install
npm install -g <package_name>

# Uninstall
npm uninstall -g <package_name>

# Publish
npm login
npm publish